projectName: endr
owners:
  - calavelas
config:
  git:
    provider: github
    owner: calavelas
    repo: ENDR
    defaultBranch: main
  environments:
    kubernetes:
    - name: gargantua
      description: k3d cluster (Interstellar-themed)
  namespace:
    - name: miller
    - name: mann
    - name: edmunds
templates:
  extends: []
  service:
    - name: python-fastapi
      type: service
      description: FastAPI service template
      path: ENDR/engine/templates/service/python-fastapi
    - name: endr-robot
      type: service
      description: Demo robot — personality via env, runs the shared endr-robot image
      path: ENDR/engine/templates/service/endr-robot
  gitops:
    - name: helm-service
      type: gitops
      description: Helm chart and ArgoCD app template
      path: ENDR/engine/templates/gitops/helm-service
    - name: helm-service2
      type: gitops
      description: Helm chart and ArgoCD app template
      path: ENDR/engine/templates/gitops/helm-service
