projectName: endr
owners:
  - calavelas
config:
  git:
    provider: github
    owner: calavelas
    repo: ENDR
    defaultBranch: main
  environments:
    kubernetes:
    - name: mac-lab
      description: Testing K8s cluster on Mac
  namespace:
    - name: demo
    - name: demo2
templates:
  extends: []
  service:
    - name: python-fastapi
      type: service
      description: FastAPI service template
      path: ENDR/engine/templates/service/python-fastapi
    - name: endr-robot
      type: service
      description: Demo robot — personality via env, runs the shared endr-robot image
      path: ENDR/engine/templates/service/endr-robot
  gitops:
    - name: helm-service
      type: gitops
      description: Helm chart and ArgoCD app template
      path: ENDR/engine/templates/gitops/helm-service
    - name: helm-service2
      type: gitops
      description: Helm chart and ArgoCD app template
      path: ENDR/engine/templates/gitops/helm-service
