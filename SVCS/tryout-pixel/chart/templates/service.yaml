apiVersion: v1
kind: Service
metadata:
  name: tryout-pixel
spec:
  selector:
    app.kubernetes.io/name: tryout-pixel
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
