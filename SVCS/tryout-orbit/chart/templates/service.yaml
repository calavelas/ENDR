apiVersion: v1
kind: Service
metadata:
  name: tryout-orbit
spec:
  selector:
    app.kubernetes.io/name: tryout-orbit
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
