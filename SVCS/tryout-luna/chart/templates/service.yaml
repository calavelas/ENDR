apiVersion: v1
kind: Service
metadata:
  name: tryout-luna
spec:
  selector:
    app.kubernetes.io/name: tryout-luna
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
