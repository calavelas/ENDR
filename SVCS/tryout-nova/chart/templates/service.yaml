apiVersion: v1
kind: Service
metadata:
  name: tryout-nova
spec:
  selector:
    app.kubernetes.io/name: tryout-nova
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
